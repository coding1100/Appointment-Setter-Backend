fastapi
uvicorn[standard]
python-multipart
python-dotenv
email-validator
redis
httpx
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.3.0
# Transactional email via Resend (https://resend.com/docs/send-with-python).
# Sync SDK; we call it via asyncio.to_thread so the asyncio loop stays free.
resend
twilio

# LiveKit Agents + Plugins
# Pinned for reproducible production builds. Floating versions caused
# SDK/protocol drift against LiveKit Cloud (the periodic "unexpected
# message type" reconnects). Bump deliberately, not implicitly.
livekit-agents==1.5.6
livekit-plugins-deepgram==1.5.6
livekit-plugins-elevenlabs==1.5.6
livekit-plugins-google==1.5.6
google-genai>=1.71.0
livekit-plugins-silero==1.5.6
transformers>=4.36.0
packaging>=23.2
sentencepiece>=0.1.99
livekit-plugins-turn-detector==1.5.6

# OpenTelemetry (pinned to avoid conflicts with livekit-agents)
opentelemetry-api>=1.27.0,<2.0.0
opentelemetry-sdk>=1.27.0,<2.0.0

boto3
structlog
prometheus-client
pydantic-settings
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.1.0
